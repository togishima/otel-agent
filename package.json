{
  "name": "otel-agent",
  "version": "0.2.3",
  "private": true,
  "type": "module",
  "description": "Claude Code の OpenTelemetry テレメトリをローカル受信し、localhost ダッシュボードで可視化するエージェント",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "build:sea": "scripts/build-sea.sh"
  }
}
